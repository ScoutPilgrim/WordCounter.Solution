@using WordCounter.Models;

<h1>Game History</h1>

<ul>
@if (Model.Count == 0)
{
  <p>You have played no games!</p>
}
@if(Model.Count != 0)
{
  @foreach (RepeatCounter item in Model)
  {
    <li><a href='/wordcounter/@item.GetId()'>@item.GetUserEnteredWord()</a></li>
  }
}
</ul>

<a href="/wordcounter/new">Play.</a>

<form action="/wordcounter/delete" method="post">
  <button type="submit" name="button" class="btn btn-primary">Clear All Rounds</button>
</form>
